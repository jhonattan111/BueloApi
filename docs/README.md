# docs/

| Path | What |
|---|---|
| [`reference/`](reference/) | Current, authoritative reference for the declarative YAML format and `PageSettings` — one file per component |
| [`workflows.md`](workflows.md) | Short "how do I add X" recipes (endpoint, declarative block, stdlib function, store, migration) |
| [`sprint-history.md`](sprint-history.md) | Index of the sprint log below — **historical**, not current state |
| [`sprints/`](sprints/) | One file per sprint (`_archived/` = superseded by a later redesign) |

The canonical doc for architecture and conventions is [`../CLAUDE.md`](../CLAUDE.md) — it wins over
anything here on conflict. See its own "Documentation conventions" pointer (via the
[umbrella `CLAUDE.md`](../../CLAUDE.md)) for how `README.md`/`CLAUDE.md`/`docs/` divide responsibilities.
