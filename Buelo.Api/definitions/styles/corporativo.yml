kind: styles
name: corporativo
# Classes nomeadas reutilizáveis. Precedência ao resolver: inline > class > theme.
classes:
  base:       { color: "#222222" }
  empresa:    { extends: base, size: 13, bold: true }
  titulo:     { extends: base, size: 18, bold: true }
  subtitulo:  { extends: base, size: 10, color: "#666666" }
  monetario:  { align: right }
  totalLabel: { bold: true, align: right }
